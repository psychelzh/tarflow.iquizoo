tbl_data_prefixes <- c(
  raw_data = "content_orginal_data_",
  scores = "content_ability_score_"
)
name_sql_files <- c(
  project_contents = "project_contents.sql",
  project_users = "project_users.sql",
  progress_hash = "progress_hash.sql",
  raw_data = "fetch_raw_data_glue.sql",
  scores = "fetch_scores_glue.sql"
)
